{% extends 'partials/base.html.twig' %}

{% block content %}
    <div class="content wrap formdata-page">
    {{ content }}

    <div class="alert">{{ form.message }}</div>
    <p>Here is the summary of what you wrote to us:</p>

    {% include "forms/data.html.twig" %}
    </div>
{% endblock %}
